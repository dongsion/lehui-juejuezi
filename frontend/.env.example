# 前端环境变量模板
# 使用方法：cp .env.example .env.production
# 修改值后 docker compose build 时会自动注入

# 商家端登录密码（必须与后端 OWNER_TOKEN 一致）
VITE_OWNER_TOKEN=baji-owner-2026

# 骑手端登录密码（必须与后端 RIDER_TOKEN 一致）
VITE_RIDER_TOKEN=baji-rider-2026
